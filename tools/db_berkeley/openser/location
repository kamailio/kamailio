METADATA_COLUMNS
username(str) domain(str) contact(str) expires(datetime) q(double) callid(str) cseq(int) flags(int) cflags(int) user_agent(str) received(str) path(str) socket(str) methods(int) last_modified(datetime)
METADATA_KEY
0 1
METADATA_READONLY
0
METADATA_LOGFLAGS
0
