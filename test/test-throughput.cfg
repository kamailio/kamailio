debug=9			# for speed
check_via=0
dns=off
rev_dns=off
fork=no
log_stderror=yes
children=8

route{

	if ( method=~'^INV' && uri=~'iptel\.org' ){
		forward(127.0.0.1, 5060);
		drop;
	};
	log("no rule for this packet => dropping\n");
}

