all:

install:
	mkdir -p ~/.vim/ftdetect
	mkdir -p ~/.vim/syntax
	cp ftdetect/kamailio.vim ~/.vim/ftdetect/
	cp syntax/kamailio.vim ~/.vim/syntax/

# remove old files using 'ser' name
clean-ser:
	rm -f ~/.vim/ftdetect/ser.vim
	rm -f ~/.vim/syntax/ser.vim
