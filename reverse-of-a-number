#include<stdio.h>
void main()
{
    int n, reversedNumber = 0, r;
    printf("Enter an integer: ");
    scanf("%d", &n);
    while(n != 0)
    {
        r=n%10;
        reversedNumber = reversedNumber*10 + remainder;
        n/=10;
    }
    printf("Reversed Number = %d", reversedNumber);
}
